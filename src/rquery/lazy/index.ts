import { each } from "./each";
import { filter } from "./filter";
import { map } from "./map";
import { range } from "./range";

export const lazy = {
  range: range,
  each: each,
  map: map,
  filter: filter,
};
